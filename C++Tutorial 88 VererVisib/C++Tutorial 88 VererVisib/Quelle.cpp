#include <iostream>

/*"public protected private Vererbung": In C++ gibt es verschiedene Vererbungsweisen. Es kann "public", "private" und "protected" geerbt werden.
"Public": Überall sichtbar. Vererbung:
"Private": Nur in Klasse und Unterklassen sichtbar.
"Protected": Nur in Klasse selbst sichtbar.

Ist ein Element in A	public		protected	private
... wird es in B		public		protected	nicht übergeben
... wird es in C		protected	protected	nicht übergeben
... wird es in D		private		private		nicht übergeben


Man braucht so gut wie immer nur "public" in der Vererbung.
*/

int main() {



	system("pause");
	return 0;
}