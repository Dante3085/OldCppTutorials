#include <iostream>

/*"this": Ähnlich wie bei Java. Unterschied: Keine Referenz auf das eigentliche Objekt, sondern ein Pointer auf dieses.
*Anstatt"...(int size) : size(size) {}" "(*this).size = size;" Der Stern vor dem this bedeutet eine Dereferenzierung dieses Pointers.
*
*
*
*
*/

int main() {

	

	system("pause");
	return 0;
}