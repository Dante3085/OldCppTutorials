#include <iostream>

/*"Arrow Operator(Pfeiloperator)": "->" dereferenziert den Pointer links von sich und das, was rechts folgt ist direkt mit dem Pointer verwendbar.
*Beispiel "(*this).size = size;" == "this -> size = size".
*
*
*
*/

int main() {



	system("pause");
	return 0;
}